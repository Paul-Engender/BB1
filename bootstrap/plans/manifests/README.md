# Planning Manifests

This folder holds machine-readable planning-control manifests.

Current manifest:

- `plans_manifest.csv`

Rule:

- Manifest rows classify planning documents by scope, role, lifecycle state, and replacement chain.
- The manifest is execution-plane control metadata and does not change canonical governance authority in `specs/`.
