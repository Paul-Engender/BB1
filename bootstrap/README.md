# Bootstrap Execution Layer

Active bridge controls were moved to `bootstrap/bridge/`.

Use:
- `bootstrap/bridge/README.md` for bridge operation guidance
- `bootstrap/kernel_pm/bundles/` for forward bundle-first authority
- `bootstrap/kernel_pm/projections/` for deterministic read views
