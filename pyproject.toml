[tool.poetry]
name = "root-project"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.11" # Assuming Python 3.11 based on ai_gateway's pyproject.toml
ai_gateway = { path = "gateways/ai_gateway", develop = true }

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
